{
  "name": "cv-review-app",
  "version": "1.0.0",
  "description": "AI-powered CV and cover letter review tool",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "build:js": "esbuild js/entry.js --bundle --outfile=js/bundle.js --platform=browser --target=es2017"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.18.0"
  },
  "type": "module",
  "engines": {
    "node": "18.x"
  }
}
